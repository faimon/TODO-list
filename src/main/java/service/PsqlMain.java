package service;

import persistence.Task;
import persistence.User;

import java.sql.Timestamp;

public class PsqlMain {
    public static void main(String[] args) {
//        var task = new Task("desc", new Timestamp(System.currentTimeMillis()), false);
//        var user = new User("bot", "pass");
//        PsqlStore.instOf().saveUser(user);
//        PsqlStore.instOf().addTask(task, user);
    }
}
